<?php

/**
 * Implements hook_block_info
 */
/*
function sirius_pcct_block_info() {
  $blocks['sirius_pcct_policy_info'] = array(
    'info' => t('Sirius PCCT Policy Info'),
    'cache' => DRUPAL_CACHE_PER_ROLE,
  );

  return $blocks;
}
*/

/**
 * Implements hook_block_view
 */

/*
function sirius_pcct_block_view($delta = '') {
  if ($delta == 'sirius_pcct_policy_info') {
    $block['subject'] = t('My Benefit Summary');
    $block['content'] = sirius_pcct_policy_render();
  }

  return $block;
}
*/
